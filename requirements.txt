# --- Core (required) ---
# Python client for the Ollama runtime. NOTE: this is only the client library.
# The Ollama application/server itself must be installed separately (see README),
# and you must pull a model (e.g. `ollama pull gemma2`) before running the pipeline.
ollama>=0.5